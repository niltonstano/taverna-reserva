# 🍷 Taverna Reserva — API de E-commerce de Alta Performance

API de e-commerce desenvolvida com foco em **resiliência, consistência de dados e segurança**, utilizando uma arquitetura escalável com **Fastify, MongoDB (Replica Set) e TypeScript**.

O projeto implementanda soluções para problemas reais de sistemas distribuídos, como **concorrência de estoque e idempotência de pagamentos**.

---

## 🚀 Diferenciais de Engenharia (O que torna este projeto único)

Diferente de APIs comuns, este projeto implementa padrões de sistemas de missão crítica:

### 🛡️ Consistência de Dados e Transações ACID

Utiliza **MongoDB Replica Set** para garantir transações atômicas. No processo de checkout, a criação do pedido, a baixa de estoque e a limpeza do carrinho ocorrem em uma única transação: **ou tudo acontece, ou nada é alterado**.

### ⚡ Proteção contra Condição de Corrida (Race Conditions)

O sistema de estoque foi validado com **testes de estresse simultâneos**. Se 10 usuários tentarem comprar o último item ao mesmo tempo, o sistema garante através de travas lógicas e transações que apenas 1 consiga, mantendo a integridade do inventário.

### 🔑 Idempotência de Operações

Implementação de **Idempotency Keys** em rotas críticas (Checkout). Isso evita que uma falha de conexão ou um "duplo clique" do usuário resulte em duas cobranças ou dois pedidos idênticos.

### 🧪 Suíte de Testes Profissional

- **Testes de Integração:** Validando o fluxo completo com banco de dados em memória.
- **Testes de Estresse:** Simulação de múltiplas requisições simultâneas para validar concorrência.
- **Segurança (Anti-IDOR):** Testes específicos que garantem que um usuário não consiga acessar dados de terceiros.

---

## 🛠️ Stack Tecnológica

| Tecnologia               | Finalidade                                                       |
| :----------------------- | :--------------------------------------------------------------- |
| **Node.js 24 + Fastify** | Runtime e Framework de alta performance (low overhead).          |
| **TypeScript**           | Desenvolvimento tipado e seguro.                                 |
| **MongoDB + Mongoose**   | Banco NoSQL com modelagem via Schemas e suporte a Transações.    |
| **Zod**                  | Validação de esquema e contratos de dados (Type-safety runtime). |
| **JWT + Bcrypt**         | Autenticação robusta e segurança de credenciais.                 |
| **Melhor Envio API**     | Integração real para cotação de frete e logística.               |
| **Docker & Compose**     | Orquestração de containers para API e Banco de Dados.            |

---

## 🏗️ Arquitetura e Padrões de Projeto

A aplicação segue os princípios de **Clean Architecture** e **SOLID**:

- **Controllers:** Apenas manipulação de entrada e saída (REQ/RES).
- **Services:** Toda a regra de negócio, isolada e testável.
- **Repositories:** Abstração da camada de dados (Data Access Layer).
- **Middlewares/Hooks:** Saneamento de dados automático e logs estruturados com **Pino**.

---

## 📦 Funcionalidades Principais

- **Gestão de Produtos:** Controle de estoque rigoroso e catálogo dinâmico.
- **Checkout Inteligente:** Integração com **Melhor Envio** para cotação de frete em tempo real baseada em dimensões e peso.
- **RBAC (Role Based Access Control):** Níveis de permissão distintos para `Admin` e `Customer`.
- **Logs Estruturados:** Monitoramento de saúde e erros da API em tempo real.

---

## 📊 Observabilidade e Resiliência (Logs e Diagnósticos)

O projeto utiliza o **Pino** para geração de logs estruturados em JSON. Durante a execução da suíte de testes ou em produção, você poderá observar logs de nível `WARN (40)` ou `ERROR (50)`.

É importante destacar que:

- **Erros Induzidos:** Grande parte desses logs são comportamentos esperados e induzidos pelos testes de segurança e estresse.
- **Validação de Bloqueio:** Logs de "Tentativa de acesso não autorizado" ou "BLOQUEIO RBAC" confirmam que as camadas de proteção estão interceptando acessos indevidos com sucesso.
- **Controle de Concorrência:** Logs de erro em operações de checkout simultâneas são evidências de que o mecanismo de idempotência travou uma requisição duplicada para proteger a integridade financeira da operação.

## "Em um sistema resiliente, um log de erro bem rastreado é a prova de que uma falha foi prevista e contida."

## 🔧 Como Executar

Clonar e Configurar:

Bash
git clone https://github.com/niltonstano/taverna-reserva.git
cd taverna-reserva
cp .env.example .env # Não esqueça de preencher as variáveis!

Subir Containers:
O projeto conta com um serviço de auto-inicialização para o MongoDB Replica Set.

Bash
docker compose up -d --build

docker logs taverna_api

Popular o Banco (Seed):
Essencial para a primeira execução para criar o usuário Admin e o catálogo inicial:

Bash
docker exec -it taverna_api npx tsx src/scripts/seed-master.ts

Principais endpoints
API Docs (Swagger): http://localhost:3333/docs

Health Check: http://localhost:3333/api/v1/health

Frontend: http://localhost:8080

```

```

## Como executar os testes

npm run test:manual-stress
OBs:(Com IDs validos)

Para validar tudo: npm test

Para focar em segurança/IDOR: npm run test:int

Para verificar o deploy final: npm run test:e2e

👨‍💻 Autor
Nilton Alexandre Stano Desenvolvedor Backend com foco em arquiteturas resilientes e performance.

GitHub: https://github.com/niltonstano

```

```
